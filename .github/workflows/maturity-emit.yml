name: maturity-emit

# v1.30 — caller shim. Real-time codebase-maturity emission body lives in
# atlas-hq's reusable workflow at .github/workflows/maturity-emit-reusable.yml.

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  emit:
    uses: microGRID26/Atlas-HQ/.github/workflows/maturity-emit-reusable.yml@main
    with:
      slug: microgrid
    secrets:
      MATURITY_REFRESH_FROM_GH_SECRET: ${{ secrets.MATURITY_REFRESH_FROM_GH_SECRET }}
